#include "gtest/gtest.h"

TEST(sstsIn, FunctionOnly)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsIn, FunctionParameters)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsIn, TaskId)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsIn, FunctionResult)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsIn, Disable)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsIn, Remove)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsIn, DisableEnable)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsIn, RemoveAdd)
{
	EXPECT_EQ(1 + 1, 2);
}