#include "gtest/gtest.h"

TEST(sstsAt, FunctionOnly)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsAt, FunctionParameters)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsAt, TaskId)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsAt, FunctionResult)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsAt, Disable)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsAt, Remove)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsAt, DisableEnable)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsAt, RemoveAdd)
{
	EXPECT_EQ(1 + 1, 2);
}