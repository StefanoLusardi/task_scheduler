#include "gtest/gtest.h"

TEST(sstsEvery, FunctionOnly)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsEvery, FunctionParameters)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsEvery, TaskId)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsEvery, FunctionResult)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsEvery, Disable)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsEvery, Remove)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsEvery, DisableEnable)
{
	EXPECT_EQ(1 + 1, 2);
}

TEST(sstsEvery, RemoveAdd)
{
	EXPECT_EQ(1 + 1, 2);
}